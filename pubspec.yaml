name: beads_pattern_generator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_cube: ^0.1.1
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  pdf: ^3.11.1
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
